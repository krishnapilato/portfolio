<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define log patterns -->
    <property name="FILE_LOG_PATTERN"
              value="%highlight(%-5level) [%cyan(%d{yyyy-MM-dd HH:mm:ss})] [%yellow(%thread)] %green(%logger{36}) - %msg%n%xException{full}"/>
    <property name="LOG_FILE_PATH" value="logs/application.log"/>

    <!-- Rolling File Appender -->
    <appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}-%i.zip</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>512MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- DEV profile -->
    <springProfile name="dev">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ROLLING"/>
        </root>
    </springProfile>

    <!-- PROD profile -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="ROLLING"/>
        </root>
        <logger name="com.personal" level="DEBUG" additivity="false">
            <appender-ref ref="ROLLING"/>
        </logger>
    </springProfile>

</configuration>